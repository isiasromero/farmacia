extends layout
block content
  .container.intro-med
    div(class='row')
      if message.length > 0
        div(class='alert alert-success' role='alert')= message
      -for(var i=0; i<maquillaje.length; ++i){
        .col-sm-6.item-med
          form(action='http://localhost:8080/editar-maquillaje' method='post' autocomplete='off')
            input#id_producto(type="hidden" name="id" value=maquillaje[i].id)
            h4=maquillaje[i].nombre
            img(src="img/#{maquillaje[i].imagen}", width="200")
            p="Descripción: " + maquillaje[i].descripcion
            strong="$"+maquillaje[i].precio
            br
            i="Cantidad: " + maquillaje[i].cantidad
            div.container-btn
              button.btn-edit.btn-success(type="submit")
                span.glyphicon.glyphicon-pencil
              a.btn-edit.btn-danger.eliminar.delete-item(href="javascript:void()")
                span.glyphicon.glyphicon-trash
      -}
